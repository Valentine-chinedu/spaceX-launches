export interface capsules {
	reuse_count: string;
	water_landings: number;
	land_landings: number;
	last_update: string;
	launches: Array<string>;
	serial: string;
	status: string;
	type: string;
	id: string | number;
}
